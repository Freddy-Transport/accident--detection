[project]
name = "traffic-accident-rnd"
version = "0.1.0"
description = "MVP for early traffic accident discovery in fixed urban road surveillance videos."
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
addopts = "-q"
